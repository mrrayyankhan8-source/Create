const fs = require('fs');
const path = require('path');
const data = `[
    {
        "title": "Acacia Planks",
        "url": "https://create.fandom.com/wiki/Acacia_Planks"
    }
]`; // Too big to inline, I'll just write a script to download it or skip it since I have it in prompt context... Wait, the prompt context gave me the big JSON. I can save it if needed, but it's massive.
